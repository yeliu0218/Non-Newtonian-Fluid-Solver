MODULE CONFIG
   run_options = < "-Call" >
   condition_for_execution = is_defined( "BS_with_MPI" )
   mpi_machinefile = vector( host_name(), host_name() ) 
   mpi_options = < "-np" "2" >
END MODULE CONFIG
